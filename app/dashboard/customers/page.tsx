import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Customers"
}
const CustomersPage = () => {
    return (
        <div>
            Customer Page
        </div>
    );
};

export default CustomersPage;